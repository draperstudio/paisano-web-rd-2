# Settled — off the consideration table

Decisions here feed the run prompt as constraints, never as considerations.
Filled during the board pass.

## Colors

Near-locked. Values pulled programmatically from the round-one exploration frames on the board (the ones Buck kept and grouped), 7.12.26. **Buck confirms these exact values before they harden** — the family is right, the hex points are what the explorations actually used.

- **Maroon (brick)** `#641D18` — the dominant ground of the script explorations; surface and text both
- **Deep wine** `#42101B` — the darker burgundy, used as ground and text
- **Cream** `#F4EFE3` (drift range seen: `#F4EFE4`, `#F2EDE1`, `#F5F0E8`) — the light ground and the light text on maroon
- **Parchment / darker cream** `#E8DFD0`, `#DCD3C0` — secondary light surfaces
- **Warm ink (near-black)** `#221F19` / `#1B1916` — dark text on light grounds
- **Stone (muted warm gray)** `#6F6759` / `#6E675C` — secondary and micro text on light grounds

The cream family drifts across four near-identical values in the explorations — one value per role should be picked when this locks.

## Type

**Not landed — explicitly.** The board pass expectation was that type might land as 2–3 locked directions, but Buck's band commentary says the opposite throughout: "there's a lot of fine nuanced je ne sais quoi that needs to be figured out as far as what the typefaces we use are and the kerning and the sizing" (band 07), "we need to try some different typefaces" (band 09), "type selection, the case, the kerning, the sizing is totally fucking off and needs to be fucking dialed" (band 20). Typeface selection, kerning, sizing, and casing stay open work across nearly every expression. Nothing goes here until directions are actually picked and fonts pulled.
