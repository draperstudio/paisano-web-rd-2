# Round — v0 — 7.12.26

**Expression:** 20 card on image
**Status after round:** open — variations live, needs Buck's eye

## What was tried

Three live variations of the two-layer structure (background image
ground + framed card with image one side / text the other), rebuilt
from zero — the kept exploration's image and type choices were
explicitly not a base:

- `/e20/var-1` — full-bleed courtyard background, card image-LEFT
- `/e20/var-2` — full-bleed courtyard background, card image-RIGHT
- `/e20/var-3` — CONTAINED: the background gets the brand's held
  parchment frame instead of bleeding (the other side of Buck's
  "full bleed is better here than other places but not loved" hedge)

## Calls made (all provisional, mine)

- Background: the courtyard fountain photo (current CDN set). Card
  image: the lobby arch photo — interior-inside-exterior gives the
  layers an actual relationship instead of two random photos.
- Card interior follows 18's working proposal (typewriter kicker /
  serif title / serif body), CENTERED — the off-to-left eyebrow read
  weird in the kept exploration, and the eyebrow LINE is dead (killed
  here and in 17). No line anywhere.
- Content: courtyard dinner in the warm months — dossier-backed.
- Card takes a real shadow (shadow-lg) — it must read as floating ON
  the image, not printed into it; that separation is the expression.

## Where it can be seen

`/e20/var-1`, `/e20/var-2`, `/e20/var-3` — desktop verified.

## Open questions for Buck

- Bleed (var-1/2) or held containment (var-3)?
- Image-left or image-right?
- Does the 18-style card interior read right here, or does this card
  want its own type logic? (Type is open brand-wide.)
