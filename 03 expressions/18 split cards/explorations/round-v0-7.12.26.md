# Round — v0 — 7.12.26

**Expression:** 18 split cards
**Status after round:** open — variations live, needs Buck's eye

## What was tried

Three live variations of the two-card composition (type card + image
card side by side), all on light ground per the one hard negative,
all carrying the kept exploration's Giant 1955 content:

- `/e18/var-1` — SEPARATE cards, image left. Buck's spacing hypothesis
  applied: the surround padding and the between-cards gap share one
  value (`clamp(1.5rem, 4vw, 4rem)`) so vertical = horizontal exactly.
- `/e18/var-2` — JOINED: one bordered card, the split internal.
  Tests the other side of "I don't exactly love that the cards are
  separate but also maybe it's okay."
- `/e18/var-3` — separate cards, image right — the open image-side axis.

## Calls made (all provisional, mine)

- Light ground = parchment surround with cream cards — "not dark" is
  the only locked color statement, so this stays inside the settled
  palette rather than inventing a new "light."
- Type card interior follows the kept exploration's five levels:
  typewriter kicker / serif title with SCRIPT MID-LINE (07's move —
  "Giant" in Pinyon at 1.4em, maroon) / serif body / typewriter footer.
  Type is open brand-wide; this is one proposal, not a lock.
- Image is the current facade entrance photo from the CDN set — the
  kept exploration's historic B&W shot isn't in the media library, so
  current photography stands in until it is.
- Fixed during the round: `font-script-a` isn't a real utility — the
  tokens are `font-script-a1` (Pinyon) / `font-script-a2` (Herr Von).
  Caught in browser (the word rendered serif); corrected to a1.

## Where it can be seen

`/e18/var-1`, `/e18/var-2`, `/e18/var-3` — desktop verified.

## Open questions for Buck

- Separate (var-1/3) or joined (var-2)? The doc leaves it genuinely
  unresolved — this round puts both on screen for the call.
- Does the equal-gutter spacing fix what felt off? If not, the
  hypothesis was spacing but the disease may be the card interiors.
- Should the historic B&W facade shot be sourced for the media
  library so this section can carry it? (The doc's open question
  about hunting a reference also stands.)
