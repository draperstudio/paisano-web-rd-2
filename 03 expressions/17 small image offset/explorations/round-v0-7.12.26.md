# Round — v0 — 7.12.26

**Expression:** 17 small image offset
**Status after round:** open — variations live, needs Buck's eye

## What was tried

Three live variations of the asymmetrical split from the ref
(large anchor image one column, small offset image floating above the
text stack on the other), all on real property content — the courtyard
as anchor, arcade/neon-sign details as the small image:

- `/e17/var-1` — small image top-left of the text column,
  left-aligned stack. Caption in tracked serif microtext under the
  small image (borrows 11's caption level).
- `/e17/var-2` — centered stack per the ref's "centered stack" name:
  small image centered above kicker/title/body, all centered. The
  neon-sign detail as the small image.
- `/e17/var-3` — var-1 geometry with a counter caption
  ("Around the property — 1 of 3"), testing whether 15's numbering
  system can ride the offset image without becoming a sticker.

## Calls made (all provisional, mine)

- The small image is a REAL photograph from the current CDN set —
  never decorative filler. Its caption carries a fact.
- Offset is vertical only (image higher than the title line); no
  rotation, no scattered-polaroid gestures — settled restraint holds.
- Text column measures stay at the editorial width from 04's spread
  so the pairing could drop into World Two unchanged.

## Where it can be seen

`/e17/var-1`, `/e17/var-2`, `/e17/var-3` — desktop verified.

## Open questions for Buck

- Left-aligned (var-1) or centered (var-2) stack?
- Does the counter caption (var-3) earn its place, or is it 15's
  system leaking somewhere it doesn't belong?
