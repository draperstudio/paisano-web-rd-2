# Round — 10 worked-in text — v0 — 7.12.26

**Backfilled 7.12 (evening session):** the 7.12 build wrote no round records; this one is reconstructed from the code at `/var-1`–`/var-3` and Buck's locked decisions from that round. Everything below is (agent call, review).

## Set reviewed

No dedicated variation set. Expression 10 rode along inside the three concept worlds, one incidental deployment each:

- **All three preloaders** — the kicker line ("Marfa, Texas — Est. 1930" and siblings in `lib/preloader-configs.ts`) sits small and unframed against the giant script, off the expected slots. This is the closest thing to the move the worlds contain.
- **World one/two/three footers** — the address line worked in under the hairline, small serif, no frame.

## Tried / worked / failed

- **Tried:** only the kicker-against-giant-script placement, one axis, unvaried. Content register: real facts (est. date, address) — a choice, not a lock; the doc leaves content open.
- **Worked:** the preloader kicker earns the doc's language — small text where you wouldn't expect it, not super framed up, working against a much larger element. Comparison: the "EST" / "1930" / "Trost and Trost" bits in the expression's own round-one explorations (`explorations/Frame 2075805192.png`) — same looseness, similar scale contrast.
- **Failed / not done:** no cycling. Face, size, case, and off-slot placement inside content sections (mid-image, against a photo edge, inside a spread) were never tried. The footer address lines are conventional footer text, not really this move — listing them here at all is generous.

## Preserve

- Kicker recipe from the preloaders: 13px serif, tracked 0.18em, uppercase, ink at 70%, sitting in the top-left quadrant against script at 28vw. Reusable verbatim as one pole of the axis.

## Avoid

- Don't count ordinary footer/legal lines as 10 — the placement must be genuinely unexpected. Future builds: put the text where a designer wouldn't (inside the image field, hanging off a column, mid-composition).

## Fault line

- **Axis finding:** the expression is essentially untested — one incidental deployment, zero cycles. Not a doc problem; the doc's open axes are right, the worlds just never worked them.
- No doc faults found. Doc unchanged at V1.

## Next move

A dedicated set (3–5 variations, one axis each: placement, face, content register) mounted in a real content section. Unblocked — no dependency gates. Queued for the fill-order pass.
