# Round — v0 — 7.12.26 (backfilled 7.12.26, agent call, review)

**Backfill notice:** reconstructed after the fact from Buck's 7.12 locked decisions and the code at `/var-2`. No round record was written at the moment of making. Everything below is "(agent call, review)".

## The set

One deployment, in World Two only (`components/world-two.tsx`): the mid-line register switch inside real running text — the rooms headline ("…and *every one* of them told straight.") and the per-room rate lines ("Rooms *from $190* a night."), script at ~1.3em against serif body.

## Tried / worked / failed

Open axes touched: **which script face** (Great Vibes — the world's script, since in Scenario B mark and moment share one gesture) and **casing of the normal text** (sentence case serif, since all-caps-with-kerning is dead per doc).

- **Worked:** the mid-line move survived contact — not killed in the 7.12 reactions; the defining lock (script mixed *within* running text, never a price sticker or floating accent) held in the build.
- **Constrained by decision 5:** the script-face pool is now finite (Pinyon, Mrs Saint Delafield, Great Vibes, plus Monsieur La Doulaise and Herr Von Muellerhoff acceptable) — the doc's "the replacement is open" now cycles inside that pool only.
- **Untested:** the doc's casing question ("sentence case or title case might not work either — we're just gonna have to try it with different typefaces") got exactly one point: sentence-case serif at 15px. One point is not a cycle.

## Preserve

- The deployment shape: script carries a *meaningful* fragment (a rate, an emphasis), never decoration; surrounding text 13–15px serif sentence case (decision 6).
- Scale relationship ~1.3em script against body — read as balanced at 1440.

## Avoid

- All caps with wide kerning for the normal text (doc negative, standing).
- Treating the script fragment as a sticker — keep it syntactically inside the sentence.

## Fault line

- **Doc problem:** none found.
- **Execution problem:** the missing round record (paid here).
- **Axis finding:** one casing point and one face point only — the casing × face cycle the doc calls for has not actually run. The texture tie (28) remains unbuilt, so this expression's fullest read stays blocked, as the doc predicted.

## Clean?

Not clean — backfill without moment-of-making captures, and the casing cycle never ran. Comparison ref: the band's script-mixed-in reference (refs/ of this folder).

## Next move

A real casing × face cycle inside the sanctioned pool, on the 28 texture when it exists — queued behind the untouched expressions in this session's fill order.
