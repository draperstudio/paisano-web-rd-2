# 05 script subtext — expressions doc

**Version:** V1 · 7.12.26 · created from the board pass (Buck's board comment, worked by AI — Buck QAs)
**Type:** specific move — one formatting treatment (script face in the subtext slot), not a full composition. Middle-column candidate per _board-pass-record.md. **No explorations exist yet.**

## What it is

The subtext below a title, set in a script face of some kind. It branched out of the Maison Charlotte reference in band 04. One move, deployable across compositions — not a layout of its own.

## What Buck likes

"What I really like here is the subtext below the title and having it in a script face of some kind. We can try that in some different variations, not all the time but sometimes cycle it in and see what we think."

## Control for (locked)

- **Type (voice-switching):** script face in the subtext slot · register change between title and subtext · type voice-switching (ad logic: "which single word gets the script")
- **Subject:** the title + subtext pair · the move lives in the subtext position specifically
- **Deployment cadence (descriptive label, not a schema control):** sometimes, not always · "sometimes cycle it in" · sparing by design

## Leave open (variation space)

- **Which script face** — open, and adjacent to the open logo-script question.
- Which compositions it cycles into — any expression with a title/subtext pair is a candidate.
- Size relationship between title and script subtext.

## Negative

- Don't make it a constant — a treatment that appears everywhere stops being a moment. (Direct from the "not all the time" instruction.)

## Open questions

- Whether this category of very-specific-move gets its own place between compositions and graphic assets — the middle-column question, Buck decides (see _board-pass-record.md).
