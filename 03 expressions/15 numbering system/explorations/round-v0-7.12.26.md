# Round — v0 — 7.12.26 (backfilled 7.12.26, agent call, review)

**Backfill notice:** reconstructed after the fact from Buck's 7.12 locked decisions and the code at `/var-1`–`/var-3`. No round record was written at the moment of making. Everything below is "(agent call, review)".

## The set

One candidate system deployed consistently inside each world (per the doc's one-system-per-surface lock):

- **Var 1:** parenthetical + leading zero — `( 01 )` on room articles, `( 01 )` on section heads (the offset-editorial ref's pattern).
- **Var 3:** parenthetical positional — `( 01 of 09 )` on the room detail panel.
- **Var 2:** no numbering — a valid null candidate.

## Tried / worked / failed

Open axis cycled: **which system** (parenthetical/leading-zero vs. parenthetical/positional vs. none). Roman numerals not yet tried (doc names them a candidate; 09 roman numerals owns that direction).

- **Held:** the consistency lock — no world mixes systems.
- **No reaction recorded:** Buck's 7.12 decisions don't touch numbering directly. The candidates stand untested against his eye; nothing here advances toward settled.

## Preserve

- `( 01 )` set in serif at 12px, ink/50 — quiet enough not to fight the title (var 1's recipe).
- `( NN of NN )` as the positional variant where a tab row gives it meaning (var 3).

## Avoid

- Mixing systems on one page — the doc's only kill, structurally enforced in the builds.

## Fault line

- **Doc problem:** none.
- **Execution problem:** the missing round record (paid here).
- **Axis finding:** the doc's note that tables are a natural surface for numbering (16 interaction) went unexplored — the 7.12 ledgers carry no numbering. Named surfaces in 16 remain the dependency for this expression's fuller round (per the handoff's dependency note).

## Clean?

Not clean — backfill, and no Buck reaction exists on the cycled axis. Comparison ref: the offset-editorial parenthetical-numbering reference in this folder's refs/.

## Next move

Roman-numeral candidate and table-surface deployment (with 16) in a later pass; this session's fill order runs untouched expressions first.
