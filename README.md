# Paisano Web — Round Two

Hotel Paisano website, round two: developing what coalesced out of round one, one notch more controlled.

**Land here first:**

- Humans: [00 - START HERE.md](00%20-%20START%20HERE.md) — what round one was, the loop this round runs, the rules that don't bend.
- Agents: [00 index.md](00%20index.md) — glossary and map; each step doc in `02 logic/` carries its own Reads/Writes header.

**State as of 7.12.26** — the board pass is done: [03 expressions/_board-pass-record.md](03%20expressions/_board-pass-record.md) has what got decided, what's open, and which calls are marked (AI, review) pending Buck's QA. 27 expression folders sit in `03 expressions/`, each with `doc.md` (locked controls vs. open variation space), `feedback.md` (Buck's raw board commentary, verbatim), `refs/`, and `explorations/`. Colors are near-locked in `01 settled/settled.md` (values pending Buck's confirm); type is explicitly not landed. Assets to make: `05 graphics/to-make.md` — the texture goes first.

**The board:** [HOTEL PAISANO — Explore page](https://www.figma.com/design/eIcOT7bW068hxTDZZi1CFO/HOTEL-PAISANO?node-id=9130-2128) — bands renamed to match the folder names here.

**For Erik in particular:** your 7.11 Depth / Motion / Time controls (in `02 logic/blocks/controls - composition.md` under "New Controls — Not Yet Vetted") picked up real use cases on the board — image cycling (01, 02), the preloader as a timed moment (01) — but weren't formally vetted band-by-band. That vet is still open, flagged in the pass record.
