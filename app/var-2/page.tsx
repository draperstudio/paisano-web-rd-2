import { WorldTwo } from "@/components/world-two"

/* Variation 2, full page, no preloader, no tab bar — for capture / Figma import */
export default function Var2Page() {
  return <WorldTwo skipPreloader />
}
