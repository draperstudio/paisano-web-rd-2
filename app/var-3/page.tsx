import { WorldThree } from "@/components/world-three"

/* Variation 3, full page, no preloader, no tab bar — for capture / Figma import */
export default function Var3Page() {
  return <WorldThree skipPreloader />
}
