import { E17Cycling } from "@/components/e17-small-image-offset"

/* Variation C — the small image cycles (band 1's version of the idea). */
export default function Page() {
  return <E17Cycling />
}
