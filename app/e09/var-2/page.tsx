import { E09Chapter } from "@/components/e09-roman-numerals"

/* Variation B — the chapter page: big letterspaced numeral + title,
   INDEX with bracketed entries, italic serif intro. */
export default function Page() {
  return <E09Chapter />
}
