import { E09Catalog } from "@/components/e09-roman-numerals"

/* Variation C — the typeface cycle Buck named: same grid, body moved
   to Cormorant (serif-two) against the typewriter metadata. */
export default function Page() {
  return <E09Catalog bodyFace="font-serif-two" />
}
