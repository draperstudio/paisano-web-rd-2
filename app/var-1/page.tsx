import { WorldOne } from "@/components/world-one"

/* Variation 1, full page, no preloader, no tab bar — for capture / Figma import */
export default function Var1Page() {
  return <WorldOne skipPreloader />
}
